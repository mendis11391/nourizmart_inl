import '../data/api_export.dart';

class AppVersionUseCase {
  final AppRepo appRepo;

  AppVersionUseCase(this.appRepo);

  // Future<CommonResponse> executeVersion(payload) async {
  //   return await appRepo.getAppVersion(payload);
  // }
}
